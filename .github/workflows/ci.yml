# name: Hybrid RAG CI/CD Pipeline

# on:
#   push:
#     branches:
#       - main

#   pull_request:
#     branches:
#       - main

# jobs:

#   build-test:

#     runs-on: ubuntu-latest

#     steps:

#       - name: Checkout Repository
#         uses: actions/checkout@v4

#       - name: Set up Python
#         uses: actions/setup-python@v5
#         with:
#           python-version: "3.12"

#       - name: Install Dependencies
#         run: |
#           python -m pip install --upgrade pip
#           pip install -r requirements.txt

#       - name: Run Tests
#         env:
#           GROQ_API_KEY: ${{ secrets.GROQ_API_KEY }}
#         run: |
#           pytest

#       - name: Build API Docker Image
#         run: |
#           docker build -f Dockerfile.api -t rag-api .

#       - name: Build Streamlit Docker Image
#         run: |
#           docker build -f Dockerfile.streamlit -t rag-streamlit .



name: Hybrid RAG CI/CD Pipeline

on:
  push:
    branches:
      - main

  pull_request:
    branches:
      - main

jobs:
  build-test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Upgrade pip
        run: python -m pip install --upgrade pip

      - name: Install API Dependencies
        run: pip install -r requirements_api.txt

      - name: Install Streamlit Dependencies
        run: pip install -r requirements_streamlit.txt

      - name: Run Tests
        env:
          GROQ_API_KEY: ${{ secrets.GROQ_API_KEY }}
        run: pytest

      - name: Build API Docker Image
        run: docker build -f Dockerfile.api -t rag-api .

      - name: Build Streamlit Docker Image
        run: docker build -f Dockerfile.streamlit -t rag-streamlit .